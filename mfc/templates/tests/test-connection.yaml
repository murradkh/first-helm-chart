apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mfc.fullname" . }}-test-connection"
  labels:
    {{- include "mfc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mfc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
